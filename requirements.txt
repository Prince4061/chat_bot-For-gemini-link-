flask>=3.0.0
flask-cors>=4.0.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
pydantic>=2.0.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
requests>=2.31.0
qrcode>=7.4.2
pillow>=10.0.0

# Production WSGI servers (waitress works on Windows + Linux; gunicorn is Linux-only)
waitress>=3.0.0
gunicorn>=21.0.0; sys_platform != "win32"

# Optional: Postgres driver when DATABASE_URL=postgresql+psycopg://...
# psycopg[binary]>=3.1

# Tests
pytest>=8.0.0
